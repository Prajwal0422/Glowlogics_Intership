Array concatination

with certical stacking and horizontal stacking whiuch allows 2 or more to add each other

np.vstack(a,b)  used to vertical stack
np.hstack(a,b) used for horizontal stack

numpy array
proveds a way to create an array

numercial string functions/

np.char.add = to add the strings to the array

numpy.char.multiply()= used to multily the data