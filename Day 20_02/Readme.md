ms-windows

ios

android

-------------------------------------------
BITS---> 0,1
-------------------------

RAM--> 8GB-->
CPU
GPU
------------------------------------------
Pandas serires.map()

the main task is used to map the values from 2 series that have a common colum. 
to map the two series should be the same as the index column of the 2nd series
the valuses should be unique
-----------------------------------------------

python data frame
is a tabular format
===============================================================
column deletion