If connected to pycahram and also connected to mysql
if created here it display database in mysql command prompt

as in second.py same
the dtable is created and in sql command prompt id desc or table shown this displays

fetchall(fetches all the data)